# rune

A minimal terminal-based text editor written in C.

Built from scratch following the kilo tutorial, with raw terminal mode, custom buffer rendering, and cursor handling — no external libraries.
